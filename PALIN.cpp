#include <iostream>
#include <string.h>

using namespace std;

void Mirror(char Base[], Array[])
{
	int n = strlen(Base);
	for(int i=0;i<n/2;i++)
		Array[i] = Base[i];
	for(int i=n/2;i<n;i++)
		Array[i] = Base[n-i-1];
}

char* NextPalindrome(char Array[])
{
	int n = strlen(Array);

	if(IsPalindrome)
	{

	}
	else
	{
		Miror()
	}
}

int main()
{
	int t;
	char Number[1000001];
	cin>>t;
	while(t--&&t>=0)
	{
		cin>>Number;

	}
}