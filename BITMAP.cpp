#include <iostream>

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	int t;
	cin>>t;
	while(t--)
	{
		int n, m;
		cin>>n>>m;
		bool map[n][m];
		for (int i = 0; i < n; i++)
			for (int j = 0; j < m; j++)
			{
				cin>>map[i][j];
				if (map[i][j])
					white.push_back()
			}
	}
	return 0;
}
