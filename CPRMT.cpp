#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	string x, y;
	while (cin>>x && cin>>y)
	{
		sort(x.begin(), x.end());
		sort(y.begin(), y.end());
		for (int i = 0; i < x.size();)
		{
			
		}
	}
	return 0;
}
