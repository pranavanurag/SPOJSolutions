#include <iostream>

using namespace std;

int main()
{
	ios::sync_with_stdio(false);

	int t;
	cin>>t;
	while (t--)
	{
		int e, f, n;
		cin>>e>>f>>n;
		int p[n], w[n], k[n];
		for (int i = 0; i < n; i++)
			cin>>p[i]>>w[i];

		
	}
	return 0;
}